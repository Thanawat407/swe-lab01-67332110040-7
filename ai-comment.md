# รายงานผลการทดลอง Lab 1

## ขั้นที่ 3: ให้ AI หา Bug
*   **AI บอกว่ามี bug กี่จุด:** 1 จุด 
*   **สรุปสั้นๆ ว่าจุดไหน:** เกิด ZeroDivisionError ในบรรทัดคำนวณ `average = total / len(scores)` เมื่อส่งลิสต์ว่าง `[]` เข้ามา เนื่องจากความยาวของข้อมูล (len) เป็น 0 ทำให้เกิดการหารด้วยศูนย์ ซึ่งในทางคอมพิวเตอร์ทำไม่ได้และจะทำให้โปรแกรมพัง

## ขั้นที่ 4: ลองกรณีที่ AI มักลืม (กรณีไม่มีคะแนนเลย)
*   **เมื่อรัน `print(calculate_grade([]))` บน Colab:** จอขึ้นสีแดง (Error) โปรแกรมพังทันที
*   **คำถาม:** เมื่อกี้ AI บอกเรื่อง bug นี้ไว้หรือเปล่า? 
*   **ตอบ:** (ให้กรอกตามจริงที่คุยกับ AI เช่น "บอก" หรือ "ไม่บอก")

## ขั้นที่ 5: สรุปสิ่งที่ได้และ Trust Level
*   **ระดับความเชื่อถือต่อ AI ในงานนี้ (1-5):** 4
*   **เพราะ:** AI สามารถชี้จุดบกพร่องและอธิบายเหตุผลหลักๆ ได้ดี แต่ในบางครั้งอาจจะไม่ได้สร้างเงื่อนไขดักจับข้อผิดพลาด (Exception Handling) มาให้ครบถ้วนทุกกรณี มนุษย์ยังคงต้องเป็นผู้ตรวจสอบและทดสอบ Edge Case (กรณีขอบเขตแปลกๆ) ด้วยตัวเองเสมอ

---

## ตาราง Trust Level (ระดับความน่าเชื่อถือของ AI)

| กรณีที่ทดสอบ (Test Case) | ผลลัพธ์จากโค้ดจริง | AI ประเมิน/อธิบาย | Trust Level | หมายเหตุ |
| :--- | :--- | :--- | :--- | :--- |
| มีคะแนนปกติ `[85, 92, ...]` | ทำงานได้ถูกต้อง (`'A'`, 87.6) | ถูกต้องตามเงื่อนไข | **High (5)** | คำนวณเกรดได้แม่นยำปกติ |
| ไม่มีคะแนนเลย `[]` | โปรแกรมพัง (ZeroDivisionError) | อาจจะเตือนหรือลืมเตือน | **Medium to Low (2-3)** | ต้องเพิ่มโค้ดเช็กเงื่อนไข `if not scores:` เพิ่มเติม |
