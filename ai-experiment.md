# รายงานผลการทดลอง Lab 1 (ใบงานฉบับย่อ)

## ขั้นที่ 1: สร้างที่รันโค้ด
* ทำการเปิด Google Colab และสร้าง New notebook เรียบร้อยแล้ว

## ขั้นที่ 2: วางโค้ดแล้วกด Run
* **โค้ดที่ใช้ทดสอบ:** โดนนำโค้ดคำนวณเกรด `calculate_grade` ชุดแรกไปวางในเซลล์ของ Colab
* **จดผลที่เห็น:** เมื่อรันรอบแรกด้วยคะแนน `scores = [85, 92, 78, 88, 95]` โปรแกรมทำงานได้ปกติและแสดงผลลัพธ์การคำนวณเกรดออกมาเฉลี่ยเท่ากับ 87.6 ได้เกรด A

## ขั้นที่ 3: ถาม AI ว่าโค้ดนี้มี bug ไหม
* **AI บอกว่ามี bug กี่จุด:** 1 จุด 
* **สรุปสั้นๆ ว่าจุดไหน:** เกิด ZeroDivisionError ในบรรทัดคำนวณ `average = total / len(scores)` เมื่อส่งลิสต์ว่าง `[]` เข้ามา เนื่องจากความยาวของข้อมูลเป็น 0 ทำให้เกิดการหารด้วยศูนย์ ซึ่งระบบคอมพิวเตอร์ไม่รองรับ

## ขั้นที่ 4: ลองกรณีที่ AI มักลืม (กรณีไม่มีคะแนนเลย)
* **เมื่อเพิ่ม `print(calculate_grade([]))` แล้วกด Run อีกครั้ง:** หน้าจอขึ้นตัวหนังสือสีแดงแจ้งเตือน Error และโปรแกรมพังทันที
* **คำถาม:** เมื่อกี้ AI บอกเรื่อง bug นี้ไว้หรือเปล่า? 
* **ตอบ:** (ใส่ตามจริง เช่น "บอกล่วงหน้าแล้ว" หรือ "ไม่ได้บอก ต้องทักท้วงเพิ่ม")

## ขั้นที่ 5: สรุปสิ่งที่ได้
* **งาน "ให้ AI หา bug" เราควรเชื่อ AI ระดับ 1 ถึง 5 เท่าไร เพราะอะไร:**
* **ระดับที่เลือก (1-5):** 4
* **เพราะ:** AI สามารถบอกจุดบกพร่องพื้นฐานได้เร็วมาก แต่บางครั้งอาจมองข้ามกรณีพิเศษ (Edge Case) เช่น ลิสต์ว่าง มนุษย์จึงยังจำเป็นต้องตรวจสอบและทดสอบระบบด้วยตัวเองเสมอ

---

## ตาราง Trust Level (ระดับความน่าเชื่อถือของ AI)

| กรณีที่ทดสอบ (Test Case) | ผลลัพธ์จากโค้ดจริง | AI ประเมิน/อธิบาย | Trust Level | หมายเหตุ |
| :--- | :--- | :--- | :--- | :--- |
| มีคะแนนปกติ `[85, 92, ...]` | ทำงานได้ถูกต้อง | ถูกต้องตามเงื่อนไข | **High (5)** | คำนวณเกรดได้แม่นยำปกติ |
| ไม่มีคะแนนเลย `[]` | โปรแกรมพัง (Error สีแดง) | อาจจะเตือนหรือลืมเตือน | **Medium to Low (2-3)** | ต้องเพิ่มโค้ดป้องกันลิสต์ว่าง |
